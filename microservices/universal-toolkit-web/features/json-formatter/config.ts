// features/json-formatter/config.ts

/**
 * Constants & Config cho JSON Formatter
 * Tool này hoạt động offline nên không có API endpoints.
 */

export const JSON_FORMATTER_CONFIG = {
  defaultIndentSize: 2 as number,
  indentOptions: [2, 4] as number[],
} as const;
